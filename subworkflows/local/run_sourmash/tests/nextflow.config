/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running tests for RUN_SOURMASH subworkflow
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

// Base directory for test data
// Using absolute paths to ensure tests work from any location
params {
    test_data_base = "/Users/dz11/github/ascc/assets/sourmash_testing"
    rankedlineage  = "/Users/dz11/github/ascc/rankedlineage.dmp"
    outdir         = "results"
}

process {
    // Disable publishDir for test runs to avoid cluttering
    publishDir = [
        enabled: false
    ]

    // Configure resource requirements for test runs
    withName: 'SOURMASH_.*' {
        cpus   = 1
        memory = 2.GB
    }

    withName: 'GET_TARGET_TAXA' {
        cpus   = 1
        memory = 1.GB
    }

    withName: 'PARSE_SOURMASH' {
        cpus   = 1
        memory = 1.GB
    }
}
